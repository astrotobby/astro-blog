# --- core pipeline (all free, no paid SDKs) ---
edge-tts>=7.0.0           # Microsoft Edge neural voices (>=7 fixes the 403/Sec-MS-GEC auth)
gTTS>=2.5.0               # fallback voice if edge endpoint blocks the runner IP
huggingface_hub>=0.23.0  # downloads the Wav2Lip weights in setup_wav2lip.sh
faster-whisper==1.0.3     # local caption timing (CPU ok)
requests==2.32.3
PyYAML==6.0.2
python-frontmatter==1.1.0 # parse blog post front-matter
markdown-it-py==3.0.0     # strip markdown -> plain text for the script
Pillow==10.4.0            # image sizing for ffmpeg scenes

# --- YouTube direct upload ---
google-api-python-client==2.143.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# --- other direct social posters (free, no app review) ---
PyTumblr==0.1.2           # Tumblr API v2
praw==7.7.1               # Reddit (optional, off by default)
tweepy==4.14.0            # X / Twitter v2 + chunked video upload
playwright==1.47.0        # Rumble: no upload API -> headless-browser upload (needs `playwright install chromium`)

# FFmpeg is a system binary, NOT pip. Install separately:
#   Windows:  winget install Gyan.FFmpeg
#   Linux:    apt-get install -y ffmpeg   (the GitHub Action does this)
